import { Button } from "../Button/Button";
import { FormInput } from "../FormInput/FormInput";

export const Form = () => {
  return (
    <form>
      <FormInput />
      <FormInput />
      <Button />
    </form>
  );
};
